# دليل أوامر تثبيت وبناء المشروع على سيرفرك الخاص (بدون إعادة تثبيت Node.js)
# Project Installation & Build Commands (Using Existing Node.js on Server)

هذا الدليل مخصص لتثبيت وبناء المشروع **باستخدام بيئة Node.js الموجودة مسبقاً على سيرفرك دون إعادة تثبيتها أو المساس بها**. السكربت والأوامر تقوم أولاً بفحص الأدوات والمكتبات الموجودة، وتثبّت فقط الحزم الناقصة إن وجدت.

---

## ⚡ الخيار السريع (فحص ذكي وبناء تلقائي بضغطة واحدة)

يقوم سكربت `install.sh` تلقائياً بفحص سيرفرك:
1. يتأكد من وجود **Node.js** وإصداره دون تثبيته مجدداً.
2. يفحص مدير الحزم المتاح لديك (`npm` / `bun` / `pnpm` / `yarn`).
3. يفحص إذا كان **PM2** مثبتاً أم لا (ويثبته فقط إذا كان غير موجود).
4. يثبت اعتماديات ومكتبات المشروع فقط إذا لم تكن موجودة.
5. يبني المشروع للإنتاج (`npm run build`).
6. يشغله عبر PM2.

```bash
chmod +x install.sh
./install.sh
```

---

## 🛠️ الخيار اليدوي: الأوامر خطوة بخطوة مع الفحص المسبق

إذا كنت تفضل كتابة الأوامر بنفسك في التيرمنال:

### الخطوة 1: فحص بيئة Node.js و npm الحالية
بما أن Node.js مثبت مسبقاً لديك، تأكد من ظهوره في التيرمنال:
```bash
node -v
npm -v
```

---

### الخطوة 2: فحص وتثبيت مدير العمليات PM2 (إذا لم يكن مثبتاً)
افحص أولاً إذا كان PM2 موجوداً لديك:
```bash
pm2 -v
```
إذا أظهر رقم الإصدار، فهو موجود ولست بحاجة لتثبيته.  
إذا ظهر `command not found`، قم بتثبيته فقط:
```bash
sudo npm install -g pm2
```

---

### الخطوة 3: فحص أدوات النظام المساعدة (فقط إذا لم تكن مثبتة)
```bash
# للتأكد من وجود git و curl وأدوات الترجمة
which git curl make gcc >/dev/null || sudo apt install -y git curl build-essential
```

---

### الخطوة 4: الانتقال لمجلد المشروع وإعداد ملف البيئة (.env)
```bash
# الدخول لمجلد المشروع على السيرفر
cd /path/to/your/project

# فحص ملف .env وإنشاؤه إن لم يكن موجوداً
[ ! -f .env ] && cp .env.example .env

# تجهيز مجلد جلسات الواتساب وسجلات التشغيل
mkdir -p whatsapp_session logs
chmod 755 whatsapp_session logs
```

---

### الخطوة 5: تثبيت مكتبات المشروع (NPM Install)
```bash
# تثبيت حزم المشروع المحددة في package.json
npm install
```

---

### الخطوة 6: بناء المشروع للإنتاج (Build Project)
هذا الأمر يبني واجهة React بواسطة Vite ويجمع خادم Express و Baileys عبر esbuild إلى ملف الإنتاج `dist/server.cjs`:
```bash
npm run build
```
*(تأكد من إنشاء المجلد `dist/` وملف `dist/server.cjs`)*

---

### الخطوة 7: تشغيل التطبيق في الخلفية بواسطة PM2
```bash
# تشغيل التطبيق عبر ملف الإعداد المرفق
pm2 start ecosystem.config.cjs

# حفظ حالة التطبيق ليعمل تلقائياً عند إعادة تشغيل السيرفر
pm2 save
pm2 startup
```

---

### الخطوة 8: فحص حالة التطبيق والتأكد من عمله
```bash
# عرض حالة التطبيق
pm2 status

# فحص استجابة الخادم
curl -I http://localhost:3000/api/health

# عرض سجلات التشغيل المباشرة
pm2 logs laundry-app
```

---

## 🔄 أمر التحديث السريع عند تعديل الكود مستقبلاً
عند تعديل أو سحب أي كود جديد:
```bash
chmod +x deploy.sh
./deploy.sh
```
أو يدوياً:
```bash
git pull
npm install
npm run build
pm2 restart laundry-app
```
